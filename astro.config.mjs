// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// site: your GitHub Pages URL. base: your repo name (if not a user/org root site).
// e.g. if repo is github.com/emmairwin/oliver → site + base below are correct.
// If you set up a custom domain, remove `base` and update `site`.
export default defineConfig({
  site: 'https://ollie-garden.com',
});
